import styled from "@emotion/styled";

export default styled.main`
  /* * {
    box-sizing: border-box;
    margin: 0;
  }

  body {
    font-family: Montserrat, sans-serif;
    color: black;
    line-height: 1.59;
    background: $white;
    font-weight: 500;
    text-rendering: optimizeLegibility;
    font-size: calc(14px + (20 - 14) * ((100vw - 320px) / (2100 - 320)));
  }

  .Main {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
  }

  .Content {
    flex-grow: 9999;
    width: 90%;
  }

  .text-wrapper {
    padding-left: 4em;
    padding-right: 3em;
    margin-bottom: 2em;
  }

  .Title {
    margin: 3em auto 1em;
    text-align: center;
  }

  h1,
  h2,
  h3 {
    font-weight: 700;
    line-height: 1.45;
  }

  h1 {
    font-size: 4em;
    font-weight: 700;
  }

  h2 {
    font-size: 2.8em;
    font-weight: 700;
  }

  h3 {
    font-size: 1.8em;
    line-height: 1.45;
    margin-bottom: 0.3em;
  }

  h4,
  h5,
  h6 {
    line-height: 1.45;
  }

  p {
    line-height: 1.55;
  }

  p.large {
    font-size: 1.15em;
    margin-bottom: 1.1em;
  }

  ul {
    margin-block-start: 0;
    margin-block-end: 0;
    padding-inline-start: 0;
  }

  hr {
    margin: 1.5em 0;
  }

  .fancyAND {
    $main-color: ${(props) => props.theme.color.primary};
    color: $main-color;
    display: block;
    text-transform: uppercase;
    font-size: 0.9em;
    font-weight: 800;
    position: relative;
    padding-top: 0;
    padding-bottom: 2px;
    margin: 9px 0.6em 0;

    &:before {
      content: "";
      display: block;
      position: absolute;
      background-color: $main-color;
      height: 3px;
      width: 100%;
    }
    &:before {
      top: 0;
      left: 0;
    }
  }

  blockquote {
    $quote-mark: 1.2em;
    display: block;
    max-width: 20em;
    margin: 3em auto;
    padding-left: ($quote-mark * 4);
    position: relative;
    color: ${(props) => props.theme.color.greyMed};
    font-size: 1em;
    font-weight: 500;
    font-style: italic;
    line-height: 1.5;

    &:before {
      content: "";
      color: ${(props) => props.theme.color.greyLt};
      font-size: 3em;
      position: absolute;
      background: url(../images/svgs/quote.svg);
      background-repeat: no-repeat;
      width: $quote-mark;
      height: $quote-mark;
      left: 0;
      top: 0.1em;
    }
  } */
`;
