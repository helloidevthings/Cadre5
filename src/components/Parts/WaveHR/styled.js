import styled from "@emotion/styled";

export default styled.figure`
  display: block;
  overflow: hidden;
  max-width: 100vw;
  figure {
  }
  svg {
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    vertical-align: middle;
  }
`;
